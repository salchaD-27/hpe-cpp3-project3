services:
  vlstorage:
    image: victoriametrics/victoria-logs:v1.49.0
    container_name: node-6-vlstorage-2
    command:
      - "-storageDataPath=/storage"
      - "-retentionPeriod=365d"
      - "-httpListenAddr=:9428"
    volumes:
      - ./storage:/storage
    ports:
      - "8003:9428"
    restart: unless-stopped
    networks:
      - multi-node-net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  multi-node-net:
    external: true