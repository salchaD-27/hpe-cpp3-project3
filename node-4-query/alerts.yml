groups:
  - name: multinode-alerts
    interval: 30s
    type: vlogs
    rules:
      - alert: LogFlowStopped
        expr: '* | stats count()'
        for: 2m
        annotations:
          summary: "No logs flowing"
          description: "No logs have been received in the last 2 minutes"
        labels:
          severity: warning
