groups:
  - name: multinode-alerts
    interval: 30s
    type: vlogs
    rules:
      - alert: HasErrors
        expr: '* | count()'
        for: 0s
        annotations:
          summary: "Logs detected"
          description: "Logs are flowing: {{ $value }}"
        labels:
          severity: info
