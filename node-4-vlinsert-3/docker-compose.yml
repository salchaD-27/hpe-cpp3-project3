services:
  vlinsert:
    image: victoriametrics/victoria-logs:v1.49.0
    container_name: node-4-vlinsert-3
    command:
      # Only writes to vlstorage-3
      - "-storageNode=node-7-vlstorage-3:9428"
      - "-httpListenAddr=:9428"
      - "-select.disable=true"
    ports:
      - "8013:9428"
    restart: unless-stopped
    networks:
      - multi-node-net
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"

networks:
  multi-node-net:
    external: true