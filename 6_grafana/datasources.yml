# VictoriaLogs datasource for Grafana
# Plugin: victoriametrics-logs-datasource v0.26.2
# Install plugin first: unzip into /var/lib/grafana/plugins/
# Add to /etc/grafana/grafana.ini under [plugins]:
#   allow_loading_unsigned_plugins = victoriametrics-logs-datasource

name: VictoriaLogs
type: victoriametrics-logs-datasource
url:  http://localhost:9428
